apiVersion: v1
kind: Service
metadata:
  name: my-website-portfolio
spec:
  selector:
    app: my-website-portfolio
  ports:
    - name: frontend
      port: 80
      targetPort: http
    - name: backend
      port: 5000
      targetPort: 5000
  type: LoadBalancer
