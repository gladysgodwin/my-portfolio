apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-portfolio
  template:
    metadata:
      labels:
        app: my-portfolio
    spec:
      containers:
      - name: frontend
        image: gladysgodwin/portfolio_frontend:latest
        ports:
        - containerPort: 8090
      - name: backend
        image: gladysgodwin/portfolio_backend:latest
        ports:
        - containerPort: 9696
        env:
          - name: SERVER_URL
            value: http://my-portfolio:3000/submit-data
          - name: POSTGRES_HOST
            value: db
          - name: POSTGRES_USER
            value: postgres
          - name: POSTGRES_PASSWORD
            value: gladys
